---
from: pm
to: developer
date: 2026-03-25
time: 16:00
priority: P1
---

## 변경 사항
- F4 UI 방향 확정: 기존 등록 모달에 자연어 입력 필드 추가
- F5 신규: SVG + CSS 스크랩 레이아웃 코디 비주얼
- 이미지 생성 PoC → **취소** (비용 문제로 SVG 방식으로 전환)
- 이전 핸드오프(2026-03-25-1530)의 Gemini 이미지 PoC는 **불필요**

## 다음 작업 지시

### 1단계: F4 Edge Function — `chat-register` (선행, 디자인 대기 불필요)

`supabase/functions/chat-register/index.ts` 생성

**요청**:
```typescript
POST /functions/v1/chat-register
{
  messages: [
    { role: "user", content: "검정 패딩이랑 베이지 니트 등록해줘" }
  ]
}
```

**응답**:
```typescript
{
  message: "2개 등록할게요!",
  items: [
    { categoryId: "uuid", categoryName: "패딩", categoryIcon: "🧥", type: "아우터", color: "#333333", colorName: "검정" }
  ],
  needsClarification: false,
  suggestions: []  // 되물을 때: ["코트", "자켓", "니트"]
}
```

**Gemini 프롬프트 설계**:
- clothing_categories 전체 목록 + 색상 프리셋 13개를 system prompt에 포함
- 멀티턴 지원 (messages 배열)
- 불확실하면 needsClarification: true
- 기존 `recommend/index.ts` 패턴 참고

### 2단계: F4 프론트엔드 (디자인 완료 후)

- 기존 `add-clothing-modal.js`에 자연어 입력 영역 추가
- 새 서비스: `src/services/chat-register.service.js`
- 기존 `closet.service.js`의 `addClothing()` 재사용

### 3단계: F5 SVG 에셋 + 스크랩 레이아웃 (F4 이후)

- 오픈소스 SVG 의류 일러스트 수집 (SVGRepo, Lucide 등)
- 카테고리 30개 → SVG 매핑 상수 테이블
- CSS fill로 색상 동적 적용
- 코디 추천 결과를 스크랩 레이아웃으로 렌더링 (transform rotate, z-index, shadow)

### 참고 파일
- `supabase/functions/recommend/index.ts` — 기존 Gemini 패턴
- `src/services/closet.service.js` — addClothing() 재사용
- `src/components/organisms/add-clothing-modal.js` — 수정 대상
- `docs/specs/requirements.md` — F4, F5 전문
