---
from: designer
to: developer
date: 2026-03-25
time: 17:00
priority: P1
---

## 변경 사항

### [중요] F4 UI 방향 변경 — 별도 채팅 화면 → 모달 내 탭

PM 방향 변경에 따라 **별도 채팅 화면을 폐기**하고, 기존 옷 등록 모달에 **"직접 선택 / AI 입력" 탭 전환 구조**를 추가했습니다.

### F4 산출물
- **`screens/add-clothing-modal-v2.html`** — 탭 통합 모달 (직접 선택 + AI 입력)
- 기존 수동 등록 UI는 "직접 선택" 탭에 그대로 유지
- "AI 입력" 탭: 자연어 입력창 + AI 대화 + 파싱 카드 + 되묻기 칩 + 성공 피드백
- `closet-page.html`의 "+" 버튼 → 모달 오픈 (탭 전환은 모달 내부)
- ~~`chat-register.html`, `chat-register-desktop.html`~~ → 참고용으로만 보존

### F5 산출물 (신규)
- **`screens/outfit-scrap-layout.html`** — 매거진 스크랩 스타일 코디 추천 비주얼
- SVG 일러스트 + CSS fill 색상 동적 적용
- 비대칭 배치 (rotate, absolute positioning, overlap)
- 테이프 장식, 배경 그리드 텍스처

## 다음 작업 지시

### F4 구현
1. **모달 탭 전환** — "직접 선택" / "AI 입력" 탭 UI 추가
   - 기존 add-clothing-modal 컴포넌트에 탭 상태 관리 추가
   - "AI 입력" 탭 내부: 채팅형 대화 UI (Lit 컴포넌트)
2. **AI 입력 탭 컴포넌트**:
   - `ai-chat-input` — 입력바 + 전송 + 빠른 제안 칩
   - `ai-message-bubble` — AI/User 메시지 버블
   - `parsed-item-card` — 파싱 결과 카드 (이모지 + 색상 도트)
   - `suggestion-chips` — 되묻기 선택지 (pill 칩)
3. **Supabase Edge Function** `chat-register` — Gemini 자연어 파싱
4. 기존 `addClothing()` 재사용하여 등록

### F5 구현
1. **SVG 에셋** — 30개 카테고리별 단색 SVG (`src/assets/clothes/`)
2. **ScrapLayout 컴포넌트** — 4개 아이템 비대칭 배치
   - `scrap-outfit-card` — 개별 스크랩 카드 (SVG + 색상 + 기울기)
   - CSS transform rotate, z-index layering
   - 테이프 장식 (CSS pseudo-element 또는 별도 요소)
3. **SVG 색상 동적 적용** — CSS `fill` 또는 SVG 내 `currentColor` 활용
4. **폴백** — SVG 없는 카테고리는 이모지 표시
5. 홈 페이지의 기존 grid 4칸 → ScrapLayout으로 교체

### 참고 파일
- `docs/design/components.md` — 섹션 9~12
- `docs/design/screens/add-clothing-modal-v2.html` — 탭 통합 모달
- `docs/design/screens/outfit-scrap-layout.html` — 스크랩 레이아웃
- `docs/specs/requirements.md` — F4, F5 요구사항
