---
from: developer
to: tester
date: 2026-04-01
time: 10:00
priority: P0
---

## 변경 사항

PM 핸드오프(에러 핸들링 보강)를 반영하여 6건 수정 완료.

### P0 — 버그 위험 수정
1. **closet.service.js 세션 null 체크** — `getUserClothes`, `addClothing`, `deleteClothing` 모두 세션 null 체크 추가. 미인증 시 `{ data: null, error: { message: '인증 필요' } }` 반환
2. **fetch 응답 try-catch** — `recommend.service.js`(getWeather, getRecommendation, getRecommendationWithRefresh), `chat.service.js`(sendChatMessage) 모두 try-catch 래핑

### P1 — UX 개선
3. **옷장 페이지 에러 피드백** — `closetStore.error` 활용. 로딩 실패/삭제 실패 시 error-box 표시
4. **Admin 페이지 에러 피드백** — `_error` 상태 추가. CRUD(load/save/delete/add/newType) 실패 시 에러 메시지 표시
5. **로그인 실패 피드백** — `signInWithGoogle()` 반환값 변경 (`{ error }`) + login-page에서 authStore.setError 호출
6. **복수 등록 부분 실패 처리** — `add-clothing-modal.js`: Promise.allSettled rejected 건 카운트 후 "N개 중 M개 등록 실패" 메시지 표시

### 테스트 업데이트
- `closet.service.test.js`: 세션 null 케이스 3개 TC 추가 (getUserClothes, addClothing, deleteClothing)
- 전체: 27 파일, 243 TC passed

## 수정된 파일
- `src/services/closet.service.js`
- `src/services/recommend.service.js`
- `src/services/chat.service.js`
- `src/services/auth.service.js`
- `src/components/pages/closet-page.js`
- `src/components/pages/admin-categories-page.js`
- `src/components/pages/login-page.js`
- `src/components/organisms/add-clothing-modal.js`
- `tests/services/closet.service.test.js`

## 다음 작업 지시
- 기존 240 TC + 신규 3 TC = 243 TC 전체 통과 확인
- 에러 핸들링 시나리오별 수동/자동 테스트 추가 검토
  - 세션 만료 상태에서 각 서비스 호출
  - 네트워크 오프라인 상태에서 fetch 호출
  - Admin CRUD 실패 시 UI 에러 메시지 표시 확인
  - 복수 등록 부분 실패 시 UI 메시지 확인
