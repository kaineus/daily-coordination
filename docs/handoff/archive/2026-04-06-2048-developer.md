---
from: developer
to: tester
date: 2026-04-06
time: 20:48
priority: P0
---

## 변경 사항

F8: 일반 코디 추천 (옷장 없이 + Pexels 이미지) 전체 구현 완료.

### Backend (Phase 1)
- `supabase/migrations/002_general_recommendations.sql` — general_recommendations 테이블 + RLS
- `supabase/functions/recommend-general/index.ts` — 비인증 일반 추천 Edge Function (Gemini + Pexels)

### Frontend 서비스/스토어 (Phase 2)
- `src/services/recommend.service.js` — `getGeneralRecommendation()`, `getGeneralRecommendationWithRefresh()` 추가
- `src/store/recommend.store.js` — `mode` 필드 + `setMode` 액션
- `src/utils/recommendation.js` — `imageUrl` 패스스루

### Auth 게이트 (Phase 3)
- `src/components/app/dc-app.js` — 비로그인 `/` (홈) 접근 허용
- `src/components/templates/app-shell.js` — 게스트 모드 (로그인 탭, 옷장/관리 숨김)

### UI 렌더링 (Phase 4)
- `src/components/pages/home-page.js` — 조건부 흐름 (비로그인→일반, 로그인+빈옷장→일반 폴백) + 유도 배너
- `src/components/organisms/outfit-card.js` — Pexels 이미지 렌더링 + onerror 폴백

### 빌드/테스트
- 빌드 성공, 27 파일 248 TC 전부 passed

## 다음 작업 지시
- 테스트 시나리오:
  1. 비로그인 상태에서 `/` 접속 → 날씨 + 일반 코디 + Pexels 이미지 표시 확인
  2. 비로그인 상태에서 "다른 코디 추천" 버튼 동작 확인
  3. 비로그인 네비게이션: "코디" + "로그인" 탭만 표시
  4. 로그인 + 옷장 있음 → 기존 F3 옷장 기반 추천 (SVG) 동작 확인
  5. 로그인 + 빈 옷장 → 일반 추천 폴백 + "옷장 등록" 유도 배너
  6. 이미지 로드 실패 시 SVG/emoji 폴백
  7. Edge Function 단독: `recommend-general` curl 테스트 (JSON 응답에 imageUrl 포함)
